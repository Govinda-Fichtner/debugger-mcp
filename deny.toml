# cargo-deny configuration for debugger-mcp
# Comprehensive dependency policy enforcement
# See: https://embarkstudios.github.io/cargo-deny/

# This section is considered when running `cargo deny check advisories`
# More documentation: https://embarkstudios.github.io/cargo-deny/checks/advisories/cfg.html
[advisories]
# The url(s) of the advisory databases to use
db-urls = ["https://github.com/rustsec/advisory-db"]
# A list of advisory IDs to ignore
ignore = [
    # Example: "RUSTSEC-2020-0001",
]

# This section is considered when running `cargo deny check licenses`
# More documentation: https://embarkstudios.github.io/cargo-deny/checks/licenses/cfg.html
[licenses]
# List of explicitly allowed licenses
# See https://spdx.org/licenses/ for list of possible licenses
allow = [
    "0BSD",
    "MIT",
    "Apache-2.0",
    "Apache-2.0 WITH LLVM-exception",
    "BSD-3-Clause",
    "BSL-1.0",
    "CDLA-Permissive-2.0",
    "ISC",
    "LGPL-2.1-or-later",
    "MPL-2.0",
    "Unicode-3.0",
    "Unlicense",
    "Zlib",
]
# The confidence threshold for detecting a license from license text
# We have high confidence that the license text is required to be accurate
confidence-threshold = 0.8

# This section is considered when running `cargo deny check bans`
# More documentation: https://embarkstudios.github.io/cargo-deny/checks/bans/cfg.html
[bans]
# Lint level for when multiple versions of the same crate are detected
multiple-versions = "warn"
# Lint level for when a crate version requirement is `*`
wildcards = "allow"
# List of explicitly disallowed crates
deny = [
    # Example: { name = "ansi_term", version = "=0.11.0" },
]
# Certain crates/versions that will be skipped when doing duplicate detection
skip = [
    # Example: { name = "ansi_term", version = "=0.11.0" },
]
# Similarly named crates that should not be considered duplicates
skip-tree = [
    # Example: { name = "windows-sys", version = "=0.42.0" },
]

# This section is considered when running `cargo deny check sources`
# More documentation: https://embarkstudios.github.io/cargo-deny/checks/sources/cfg.html
[sources]
# Lint level for what to happen when a crate from a crate registry that is not
# in the allow list is encountered
unknown-registry = "warn"
# Lint level for what to happen when a crate from a git repository that is not
# in the allow list is encountered
unknown-git = "warn"
# List of URLs for allowed crate registries. Defaults to the crates.io index
allow-registry = ["https://github.com/rust-lang/crates.io-index"]
# List of URLs for allowed Git repositories
allow-git = []
